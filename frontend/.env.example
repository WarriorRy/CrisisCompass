# .env.example for frontend (Next.js)

# Public backend URL for client-side requests (must be accessible from browser)
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000

# Internal backend URL for server-side/API route requests (used in Docker/production)
INTERNAL_BACKEND_URL=http://backend:4000
